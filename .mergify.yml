pull_request_rules:
  - name: Automatic merge for depfu pull requests
    conditions:
      - author=depfu[bot]
      - base=main
      - status-success=test
    actions:
      merge:
        method: merge
